// FileExtension
// SavePath set to SaveData file location, there should be a fallback just in case
// SaveSpecific()
// LoadSpecific()
// OnCheckCompleted()
// OnSaveSuccess()
// OnSaveFailed()

// ! EXTERNAL USE

const K_FILE_EXTENSION = "csv";
const K_FOLDER_PATH = "savedata";

function Export_Unity(){
    let temp = "";
    // License
    
    // Variable for save path
    temp = "string str_foldername = \"" + K_FOLDER_PATH + "\";";
    
    

    // End of file
    temp += "};";
}

function Export_VLPE_JS(){
}