// ? CharacterStatuses, CharacterStats, CharacterStatusEffects

let characterStatuses = new CharacterStatuses();
let characterStats = new CharacterStats();
let characterStatusEffects = new CharacterStatusEffects();

// ! --EXPORT SAVE FILE--
function export_CSV(){
    //title header
    let title = "Value,ValueDefault,ValueMin,ValueMax,RestraintMin,RestraintMax,LimitMin,LimitMax,CanRestraint,CanLimit,BonusPercentage,SystemMin,SystemMax";

    //values of GameValue 
    //(value, value default, min, max, restraintMin, restraintMax, limitMin, limitMax, canRestraint, canLimit, bonusPercentage, )
}

function export_JSON(){}
function export_YAML(){}