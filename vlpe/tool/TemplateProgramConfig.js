const programConfigs = {
    // ? Options -> Display
    isFullScreen: false,
    screenWidth: 1280,
    screenHeight: 720,
    masterVolume: 0.5,
    // ? Audio
    bgmVolume: 0.5,
    sfxVolume: 0.5,
    // ? Language
    language: "english_en",
    // ? Game
    difficulty: "normal",
    // ? HUD
    canShowMap: true,
    canShowCharacterStatuses: true,
    canShowWeaponLoadout: true,
    // ? Misc
    canSubtitles: true,
}


function exportJS();

function exportCSV();