function convertFirstLetterToUpperCase(arg_string) {
    return arg_string.charAt(0).toUpperCase() + arg_string.slice(1);
}

function convertFirstLetterToLowerCase(arg_string) {
    return arg_string.charAt(0).toLowerCase() + arg_string.slice(1);
}